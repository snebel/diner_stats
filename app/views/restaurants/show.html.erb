<h1><%= @restaurant.name %></h1>
<%= link_to 'add menu', new_restaurant_menu_path(@restaurant) %>

<h2>Menus</h2>
<ul>
  <% @restaurant.menus.each do |menu| %>
    <li><%= link_to menu.name, edit_restaurant_menu_path(@restaurant.id, menu.id)%></li>
  <% end %>
</ul>